all:
  vars:
    ansible_user: vagrant # Имя пользователя, под которым заходить на машины (тот, кому вы добавили ключ)
    # ansible_ssh_pass: your_password  # Раскомментируйте и укажите пароль, если ключ еще не настроен (плохая практика, но для начала сойдет)
    # ansible_become_pass: your_sudo_password # Пароль для sudo, если нужен
  children:
    # Группа для всех хостов
    infra_servers:
      hosts:
       # firewall:
       #  ansible_host: 192.168.100.1 # Файеревол
        web-srv:
          ansible_host: 192.168.56.10 # Веб-сервер
        db-1-srv:
          ansible_host: 192.168.56.11 # Сервер баз данных
        db-2-srv:                     
          ansible_host: 192.168.56.12 # Сервер баз данных - резервный
        mon-srv:
          ansible_host: 192.168.56.13 # Сервер мониторинга

    # Группы по ролям (для удобства)
    # firewalls:
    #   hosts:
    #     firewall:
    webservers:
      hosts:
        web-srv:
    monitoring_servers:
      hosts:
        mon-srv:
    databases:
      hosts:
        db-1-srv:
        db-2-srv: